{
  "private": true,
  "scripts": {
    "build:server": "lerna run build --scope={@airbnb_clone/common,@airbnb_clone/server}"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/rimraf",
      "**/react-native/**"
    ]
  },
  "devDependencies": {
    "lerna": "^2.11.0"
  }
}
